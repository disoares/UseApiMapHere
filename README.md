# Uso básico da API de mapas da HERE

### Para a utilização correta, basta se atentar no trecho abaixo:

```
// Altere your-api-key pela sua chave de API
var platform = new H.service.Platform({
    'apikey': 'your-api-key'
});
```